export function Home(props: { name: string; setName: (value: string) => void; seed: string; setSeed: (value: string) => void; createRoom: () => void; busy: boolean; error: string }) {
  return <section className="landing layout-grid"><div className="intro"><p className="eyebrow">LOCAL BOARD GAME · MVP</p><h1>把假設<br /><em>煮成一杯</em><br />好生意。</h1><p className="lead">一位玩家在本機驗證咖啡館策略，電腦玩家只負責合法且可重播的隨機行動。</p><div className="stat-row"><span><strong>3</strong> 時期</span><span><strong>84</strong> 張 MVP 卡</span><span><strong>1</strong> 位玩家</span></div></div><div className="panel home-panel"><p className="eyebrow">SINGLE-PLAYER MVP</p><h2>開始單機桌遊</h2><label>你的名稱<input value={props.name} onChange={(event) => props.setName(event.target.value)} maxLength={20} /></label><button className="primary full" onClick={props.createRoom} disabled={props.busy}>{props.busy ? '建立中…' : '開始本機遊戲'}</button><p className="hint">遊戲會在本機自動補足隨機電腦玩家；目前不開放區網或線上加入。</p><details><summary>測試 seed（建立房間用）</summary><input value={props.seed} onChange={(event) => props.setSeed(event.target.value)} /></details>{props.error && <p className="error">{props.error}</p>}</div></section>
}
